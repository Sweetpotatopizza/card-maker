import React from "react";
import styles from "./footer.module.css";

const Footer = () => (
    <footer className={styles.footer}>
        <p className={styles.title}>Someone in the crowd</p>
    </footer>
    )

export default Footer;
